import React, { useState } from 'react'

const AddData = () => {
    const [data, setData] = useState([])

    const Data = () => {
        setData([...data])
    }
    return (
        <div>

        </div>
    )
}

export default AddData